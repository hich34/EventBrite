<h1>Events of your city</h1>
<p>

	<% event_helper.each do |event| %>
<div class="container">
<div class="card" style="width: 18rem;">
 <div class="card-body">
   <h5 class="card-title"><%= link_to event.title, event_path(event.id) %>
   <h6 class="card-subtitle mb-2 text-muted"><%= event.price %> balles</h6>
   <p class="card-text"><%=event.description%></p>
   <h6 class="card-subtitle mb-2 text-muted">City: <%= event.location %></h6>
 </div>
 </div>
</div><br>

	<% end %>

</p>

